export const TOTAL_LEVELS = 10;
export const QUESTIONS_PER_LEVEL = 10;
export const POINTS_PER_LEVEL = 100;
export const MAX_SCORE = TOTAL_LEVELS * POINTS_PER_LEVEL; // 1000

export const STARTING_LIVES = 5;

/** Tamaño objetivo del banco de preguntas (arquitectura preparada desde el inicio). */
export const TARGET_QUESTION_BANK_SIZE = 1000;
