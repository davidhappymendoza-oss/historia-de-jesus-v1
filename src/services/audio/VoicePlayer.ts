/**
 * STUB — Narración por voz de las preguntas (ES/EN).
 *
 * No implementado todavía (ver FEATURES.voiceNarration). Cuando se
 * construya: cada Question tendrá un campo opcional `voiceOver: { es, en }`
 * con la ruta al archivo en public/voices, reproducido aquí respetando
 * settingsStore.audio.voiceEnabled/voiceVolume.
 */
export function playVoice(_questionId: string, _language: "es" | "en"): void {
  // Intencionalmente vacío.
}
